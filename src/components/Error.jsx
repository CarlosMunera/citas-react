const Error = ( { children } ) => {
  return (
    <div className="bg-red-600 text-white text-center
    uppercase font-bold p-3 w-full mb-3">
      { children }
    </div>
  )
}

export default Error
